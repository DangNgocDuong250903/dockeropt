================================================
   SỬA LỖI KẾT NỐI MẠNG
   "io: read/write on closed pipe"
================================================

❌ LỖI GỐC:
   Config VLESS + TLS sử dụng self-signed certificate
   → v2rayNG từ chối kết nối vì certificate không tin cậy

✅ ĐÃ SỬA:
   - Đổi sang VMess TCP đơn giản (không TLS)
   - Port: 10086
   - Dễ kết nối hơn, ổn định hơn

================================================
   CÁC BƯỚC SỬA LỖI TRÊN ĐIỆN THOẠI
================================================

BƯỚC 1: Xóa config cũ
   1. Mở app v2rayNG
   2. Nhấn giữ vào config "VPS-VLESS-XTLS"
   3. Chọn "Delete" (biểu tượng thùng rác)

BƯỚC 2: Import config mới
   1. Nhấn dấu + (góc trên bên phải)
   2. Chọn "Import config from QR code"
   3. Quét QR code từ file: qr-simple-new.png
   
   Hoặc copy link này và import:
   vmess:

BƯỚC 3: Kết nối
   1. Nhấn vào config "VPS-Simple"
   2. Nhấn nút ▶️ Connect ở dưới cùng
   3. → Phải kết nối được ngay!

BƯỚC 4: Kiểm tra
   1. Mở Chrome/Firefox
   2. Truy cập: https://ifconfig.me
   3. IP phải hiển thị: 34.1.141.233

================================================
   THÔNG TIN CONFIG MỚI
================================================

📋 Server:
   IP: 34.1.141.233
   Port: 10086
   UUID: 6609f6b2-faab-4bdf-8d9d-7fa518a10f31

📱 Protocol:
   - Type: VMess
   - Network: TCP
   - Security: None (không TLS)
   - AlterID: 0

⚙️ Settings khuyến nghị:
   - Routing Mode: Bypass mainland
   - Enable Mux: OFF
   - Domain Strategy: IPIfNonMatch

================================================
   NẾU VẪN LỖI
================================================

1. Kiểm tra kết nối internet điện thoại
   - Thử mở YouTube, Google xem có mạng không

2. Kiểm tra firewall
   - Port 10086 đã được mở: ✅

3. Restart app v2rayNG
   - Force stop app
   - Mở lại và thử kết nối

4. Thử tắt/bật dữ liệu di động

5. Kiểm tra server (nếu biết SSH):
   ssh -i ~/.ssh/id_rsa duongng_dn@34.1.141.233
   sudo systemctl status xray

================================================
   TẠI SAO ĐỔI SANG VMESS?
================================================

VLESS + TLS (Config cũ):
   ❌ Cần certificate hợp lệ
   ❌ Self-signed certificate bị reject
   ❌ Phức tạp hơn

VMess TCP (Config mới):
   ✅ Không cần certificate
   ✅ Đơn giản, dễ kết nối
   ✅ Vẫn đủ nhanh cho 4G/5G
   ✅ Ổn định hơn

Để có TLS đúng chuẩn, cần:
   - Domain name (tên miền)
   - Let's Encrypt certificate
   → Phức tạp hơn, không cần thiết cho mục đích tăng tốc

================================================

✅ XONG! Bây giờ quét QR code mới và kết nối nhé!

File QR code: qr-simple-new.png

================================================

